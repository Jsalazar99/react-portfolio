import React from 'react';

function Project(props) {
    return (
        <section className="card" id={this.props.id}>
            {/*<!-- Project Component -->*/}
            <a href={} target="_blank" rel="noreferrer">
                <img src={} alt={} />
            </a>
            <h4>{}</h4>
            <p>{}</p>
            <a href="{}" className="button" target="_blank" rel="noreferrer">{}</a>
        </section>
    );
}

export default Project;